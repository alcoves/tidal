{
  "watches": [
    {
      "type": "keyprefix",
      "prefix": "jobs/",
      "handler_type": "http",
      "http_handler_config": {
        "path": "http://localhost:3000/api/events/consul",
        "method": "POST",
        "header": {
          "x-foo": [
            "bar",
            "baz"
          ]
        },
        "timeout": "10s",
        "tls_skip_verify": true
      }
    }
  ]
}