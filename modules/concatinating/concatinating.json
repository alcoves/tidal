[
  {
    "name": "concatinating",
    "image": "${app_image}",
    "repositoryCredentials": {
      "credentialsParameter": "${credentials}"
    },
    "command": [
      "/bin/sh -c \"node /root/tidal/src/concatinating.js\""
    ],
    "environment": [
      {
        "name": "WASABI_ACCESS_KEY_ID",
        "value": "${wasabi_secret_access_key}"
      },
      {
        "name": "WASABI_SECRET_ACCESS_KEY",
        "value": "${wasabi_secret_access_key}"
      },
      {
        "name": "TABLE_NAME",
        "value": "${table_name}"
      },
      {
        "name": "TIDAL_ENV",
        "value": "${tidal_env}"
      }
    ],
    "cpu": 4064,
    "memory": 8128,
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "${log_group}",
        "awslogs-region": "us-east-1",
        "awslogs-stream-prefix": "ecs"
      }
    }
  },
  {
    "name": "xray-daemon",
    "image": "amazon/aws-xray-daemon",
    "cpu": 32,
    "memory": 64,
    "portMappings": [
      {
        "containerPort": 2000,
        "protocol": "udp"
      }
    ]
  }
]